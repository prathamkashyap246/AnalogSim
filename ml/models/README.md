# ML Models directory

This directory stores trained model artifacts.

## Format
- `.pkl`: Scikit-learn models
- `.pt`: PyTorch models
- `.h5`: Keras/TensorFlow models
- `metadata.json`: Model metadata (version, accuracy, etc.)

**Note**: Models are excluded from git via .gitignore
